{
  "name": "user-permissions",
  "version": "1.0.0",
  "description": "user permissions node.js",
  "main": "index.js",
  "scripts": {
    "server": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
